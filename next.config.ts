/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "static.wikia.nocookie.net",
      "upload.wikimedia.org",
      "i.imgur.com",
      "pbs.twimg.com",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
